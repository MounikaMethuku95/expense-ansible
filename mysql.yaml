- name: configure mysql server
  hosts: mysql
  become: yes
  vars:
    mysql_root_password: ExpenseApp@1
  tasks:
  - name: install python-mysql libraries
    ansible.builtin.pip:
      name: PyMysql
      executable: pip3.9

  - name: install mysql server
    ansible.builtin.package:
      name: mysql-server
      state: installed

  - name: start and enable mysql server
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes
  
  - name: connect to mysql server 
    community.mysql.mysql_info:
      login_user: root
      login_password: "{{ mysql_root_password }}"
      login_host: mysql.learndevopswithaws.cloud
    register: mysql_info
    ignore_errors: true 

  - name: print the output
    ansible.builtin.debug:
      msg: "mysql info: {{ mysql_info }}"

  - name: setup root login_password
    ansible.builtin.command: " mysql_secure_installation --set-root-pass {{ mysql_root_password }} "
    when: mysql_info.failed is true
  
  








# 1. output for install mysql server task:
# PLAY [configure mysql server] *********************************************************************************

# TASK [Gathering Facts] ****************************************************************************************
# ok: [172.31.90.161]

# TASK [install mysql server] ***********************************************************************************
# changed: [172.31.90.161]

# PLAY RECAP ****************************************************************************************************
# 172.31.90.161              : ok=2    changed=1    unreachable=0    failed=0    skipped=0    rescued=0    ignored=0

# 2. output for start and enable mysqld:
# PLAY [configure mysql server] *********************************************************************************

# TASK [Gathering Facts] ****************************************************************************************
# ok: [172.31.90.161]

# TASK [install mysql server] ***********************************************************************************
# ok: [172.31.90.161]

# TASK [start and enable mysql server] **************************************************************************
# changed: [172.31.90.161]

# PLAY RECAP ****************************************************************************************************
# 172.31.90.161              : ok=3    changed=1    unreachable=0    failed=0    skipped=0    rescued=0    ignored=0
